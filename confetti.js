// Basic confetti using canvas-confetti
// You can replace this with an external script import if desired

function launchConfetti() {
  confetti({
    particleCount: 100,
    spread: 70,
    origin: { y: 0.6 }
  });
}
